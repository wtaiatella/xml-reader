import styled from 'styled-components';

export const Container = styled.div`
	.FileName {
		font-size: 1.2em;
		margin: 1.5em 0;
	}

	.FileName strong {
		font-size: 1em;
	}

	.Container {
		display: flex;
		padding: 0 2em;
	}
`;
