import styled from 'styled-components';

export const Container = styled.form`
	flex: 1.5;
	padding-left: 4rem;

	.ButtonSubmit {
		display: block;
		width: 200px;
		height: 50px;
		font-size: 1.5em;
		margin-top: 20px;
	}
`;
